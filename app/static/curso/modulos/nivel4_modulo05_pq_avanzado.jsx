import { useState } from "react";
const V = { 50: "#F9EAED", 100: "#EDCAD0", 200: "#DCA3AD", 300: "#C97A8A", 400: "#B05A6A", 500: "#963B4D", 600: "#7B1D32", 700: "#6A1629", 800: "#520F1E", 900: "#3A0911" };
const SECTIONS = [{ title: "Merge Queries — JOINs de base de datos", content: [{ type: "definition_list", items: [{ term: "Que es", def: "Home > Merge Queries. Combina dos tablas por un campo en comun. Como XLOOKUP pero dentro de Power Query." }, { term: "Tipos de Join", def: "Left Outer (mas comun: todo de izquierda + coincidencias de derecha), Inner (solo coincidencias), Full Outer (todo de ambas), Left Anti (sin coincidencia)." }, { term: "Ejemplo", def: "Ventas con ProductID + Productos con ProductID y Name. Merge une para que cada venta tenga el nombre." }] }] }, { title: "Append Queries — apilar tablas", content: [{ type: "definition_list", items: [{ term: "Que es", def: "Home > Append Queries. Une tablas con la misma estructura una debajo de otra." }, { term: "Ejemplo", def: "Enero.xlsx + Febrero.xlsx + Marzo.xlsx consolidados en una sola tabla." }, { term: "Append as New", def: "Para 3+ tablas. Crea una query nueva con todas apiladas." }] }] }, { title: "From Folder — importar carpetas completas", content: [{ type: "definition_list", items: [{ term: "Proceso", def: "Data > Get Data > From File > From Folder. Selecciona carpeta, Power Query lista todos los archivos." }, { term: "Poder", def: "Si agregas un archivo nuevo a la carpeta, solo haces Refresh y se incluye automaticamente." }, { term: "Filtrar", def: "Puedes filtrar por extension (.xlsx, .csv) antes de combinar." }] }] }, { title: "Custom y Conditional Columns", content: [{ type: "definition_list", items: [{ term: "Custom Column", def: "Add Column > Custom Column. Usa M Language: [Revenue] - [Cost] crea Profit. Funciones: if [Amount] > 1000 then \"Alto\" else \"Normal\"." }, { term: "Conditional Column", def: "Add Column > Conditional Column. Interfaz visual sin escribir M. Define reglas como IF visual." }, { term: "Cuando usar cada una", def: "Conditional Column para logica simple. Custom Column cuando necesitas funciones M o calculos." }] }] }, { title: "Unpivot y Pivot avanzado", content: [{ type: "definition_list", items: [{ term: "Unpivot", def: "Convierte columnas en filas. Si tienes Ene, Feb, Mar como columnas, Unpivot las transforma en una columna Mes y una columna Valor. Esencial para analisis." }, { term: "Pivot", def: "Lo inverso: convierte valores de una columna en encabezados de columnas separadas. Para crear reportes con estructura especifica." }, { term: "Cuando Unpivot", def: "Siempre que recibas datos en tabla cruzada que necesites en formato tabular para PivotTables o analisis." }] }] }, { title: "Parametros y carga a Data Model", content: [{ type: "definition_list", items: [{ term: "Parametros", def: "Manage Parameters crea variables reutilizables. Ejemplo: parametro 'Year' = 2025. Las queries lo usan para filtrar. Cambias una vez, Refresh actualiza todo." }, { term: "Carga a Data Model", def: "Close and Load > Only Create Connection + Add to Data Model. Conecta Power Query con Power Pivot." }, { term: "Flujo completo", def: "Power Query importa y limpia > Power Pivot modela y relaciona > PivotTables analizan. El flujo profesional." }] }] }];
export default function Module() {
  const [openSection, setOpenSection] = useState(0);
  const renderBlock = (block, bi) => {
    if (block.type === "text") return <p key={bi} style={{ fontSize: 14, color: "var(--color-text-secondary)", margin: bi === 0 ? "0 0 14px" : "14px 0", lineHeight: 1.7 }}>{block.value}</p>;
    if (block.type === "definition_list") return (<div key={bi} style={{ display: "flex", flexDirection: "column", gap: 10, margin: "10px 0" }}>{block.items.map((item, di) => (<div key={di} style={{ background: "var(--color-background-secondary)", borderRadius: "var(--border-radius-md)", padding: "10px 14px" }}><div style={{ fontSize: 13, fontWeight: 500, color: V[600], marginBottom: 2 }}>{item.term}</div><div style={{ fontSize: 13, color: "var(--color-text-secondary)", lineHeight: 1.6 }}>{item.def}</div></div>))}</div>);
    if (block.type === "key_points") return (<div key={bi} style={{ display: "flex", flexDirection: "column", gap: 10, margin: "10px 0" }}>{block.items.map((item, ki) => (<div key={ki} style={{ borderLeft: `2px solid ${V[200]}`, paddingLeft: 14 }}><div style={{ fontSize: 13, fontWeight: 500, color: "var(--color-text-primary)", marginBottom: 3 }}>{item.title}</div><div style={{ fontSize: 13, color: "var(--color-text-secondary)", lineHeight: 1.6 }}>{item.text}</div></div>))}</div>);
    return null;
  };
  return (
    <div style={{ maxWidth: 680, margin: "0 auto", padding: "0.5rem 0 2rem" }}>
      <div style={{ marginBottom: "1.5rem" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 10 }}>
          <span style={{ fontSize: 11, fontWeight: 500, letterSpacing: "0.06em", padding: "3px 10px", borderRadius: "var(--border-radius-md)", background: V[50], color: V[800] }}>NIVEL 4 — EXCEL PRO</span>
          <span style={{ fontSize: 12, color: "var(--color-text-tertiary)" }}>Modulo 5 de 6</span>
        </div>
        <h1 style={{ fontSize: 22, fontWeight: 500, color: "var(--color-text-primary)", margin: "0 0 6px" }}>Power Query avanzado</h1>
        <p style={{ fontSize: 14, color: "var(--color-text-secondary)", margin: 0, lineHeight: 1.6 }}>Merge Queries, Append, From Folder, Custom Columns, Conditional Columns, Unpivot, parametros.</p>
      </div>
      <div style={{ background: V[50], borderLeft: `3px solid ${V[400]}`, borderRadius: "0 var(--border-radius-md) var(--border-radius-md) 0", padding: "12px 16px", marginBottom: "1.5rem" }}>
        <div style={{ fontSize: 11, fontWeight: 500, color: V[600], letterSpacing: "0.04em", marginBottom: 2 }}>OBJETIVO</div>
        <p style={{ fontSize: 13, color: V[800], margin: 0, lineHeight: 1.6 }}>Que domines transformaciones avanzadas de Power Query para flujos completos de datos.</p>
      </div>
      <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
        {SECTIONS.map((s, si) => { const isOpen = openSection === si; return (<div key={si} style={{ borderRadius: "var(--border-radius-md)", border: `0.5px solid ${isOpen ? V[200] : "var(--color-border-tertiary)"}` }}><div onClick={() => setOpenSection(isOpen ? -1 : si)} style={{ display: "flex", alignItems: "center", gap: 12, padding: "14px 16px", cursor: "pointer" }}><div style={{ width: 28, height: 28, minWidth: 28, borderRadius: "50%", background: isOpen ? V[600] : V[50], display: "flex", alignItems: "center", justifyContent: "center", fontSize: 12, fontWeight: 500, color: isOpen ? "#fff" : V[600] }}>{si + 1}</div><span style={{ flex: 1, fontSize: 15, fontWeight: 500, color: isOpen ? V[800] : "var(--color-text-primary)" }}>{s.title}</span><span style={{ fontSize: 16, color: "var(--color-text-tertiary)", transform: isOpen ? "rotate(180deg)" : "rotate(0deg)", transition: "transform 0.2s" }}>&#x25BE;</span></div>{isOpen && <div style={{ padding: "0 16px 16px 56px" }}>{s.content.map((b, bi) => renderBlock(b, bi))}</div>}</div>); })}
      </div>
      <div style={{ background: "var(--color-background-secondary)", borderRadius: "var(--border-radius-lg)", padding: "1.25rem", marginTop: "1.5rem" }}>
        <div style={{ fontSize: 14, fontWeight: 500, color: "var(--color-text-primary)", marginBottom: 10 }}>Puntos clave</div>
        {["Merge Queries = JOINs. Left Outer es el mas comun.", "Append apila tablas con la misma estructura. From Folder automatiza carpetas.", "Custom Columns usan M Language. Conditional Columns son visuales.", "Unpivot transforma columnas en filas. Esencial para datos en tabla cruzada.", "Parametros + carga a Data Model = flujo profesional completo."].map((p, i) => (
          <div key={i} style={{ display: "flex", gap: 10, alignItems: "flex-start", padding: "6px 0", borderTop: i > 0 ? "0.5px solid var(--color-border-tertiary)" : "none" }}><div style={{ width: 6, height: 6, minWidth: 6, borderRadius: "50%", background: V[400], marginTop: 7 }} /><span style={{ fontSize: 13, color: "var(--color-text-secondary)", lineHeight: 1.6 }}>{p}</span></div>))}
      </div>
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: "1.5rem", paddingTop: "1rem", borderTop: "0.5px solid var(--color-border-tertiary)" }}>
        <span style={{ fontSize: 13, fontWeight: 500, color: V[600] }}>Anterior: Conexion a fuentes externas</span>
        <span style={{ fontSize: 13, fontWeight: 500, color: V[600] }}>Siguiente: Preparacion certificacion MOS</span>
      </div>
    </div>
  );
}
